Binary-Pattern-Patcher
======================

Allows to search for matching byte patterns (regex) in a target file and replace those occurences with custom data. This allows the easy creation of binary patch files.
